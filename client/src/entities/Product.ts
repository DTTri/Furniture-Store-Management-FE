type Product = {
  id: string;
  name: string;
  category: string;
  originalPrice: number;
  productPrice: number;
  isSelling: boolean;
  stock: number;
  forSale: number;
};

export default Product;
