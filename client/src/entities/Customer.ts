type Customer = {
    id: string,
    name: string,
    phone: string,
    email: string,
}
export default Customer;
  id: number;
  name: string;
  phone: string;
  email: string;
  point: number;
};

export default Customer;
